# 更新履歴

psdtext の変更点を、**使う人から見て何が変わったか** で書く。
リリース時は「未リリース」の見出しをバージョン番号に変え、新しい「未リリース」を
上に足す。細かい経緯 (なぜそう直したか) はコミットメッセージ側に書く。

English: [CHANGELOG.md](CHANGELOG.md)

---

## 未リリース

---

## 0.2.0 — 2026-08-14

### CSV

- **CSV の取り込みが反映できなかったのを修正**。差分の一覧を作るところで
  エラーになっていて、「反映する」ボタンが押せないままになっていた
- **Excel が保存した Shift_JIS の CSV をそのまま読める**。これまでは文字化けした
  まま取り込まれていた。読み取った文字コードは画面に出る
- **書き出し先のフォルダを覚える**。取り込みのダイアログも同じ場所から開く
- **選んだレイヤだけ書き出せる** (これまではファイル内の全部だった)
- **初期書式を別の列に分けた**。フォント / サイズ / 色 / 行揃えが本文と同じセルに
  入っていると Excel から列単位で貼り付けられなかった。本文の途中で書式が
  変わっている場合は `tags` 列に残す
- 取り込み結果に「なぜ反映されないか」を出す (行が見つからない / 変化が無い など)

### 複数レイヤの作業

- **レイヤの複数選択** (Ctrl クリック / Shift クリック)
- 選んだレイヤへ**フォント・サイズ・色・行揃えの一括適用**、
  **書式のコピー &rarr; 貼り付け**
- **選んだまま複製**。レイヤ名に接頭辞を付けられるので、多言語版の下地を
  一度に作れる。マスクも一緒に複製される (CI で毎回確認している)
- **一覧 (シート) 編集**。選んだレイヤの本文と初期書式を表で並べて直せる。
  Excel からまとめて貼り付けられるので、CSV を経由しなくても差し替えられる

### フォント

- **日本語名で表示**する。右側に PSD が実際に指す PostScript 名を添えてあり、
  どちらの名前でも検索できる
- **プリセット登録** (`★`)。よく使うフォントが一番上に出る。プリセットは名前を
  付けて増やせるので、案件ごとに分けられる

### 手数を減らす

- **前回開いたフォルダを覚える**
- **レイヤ一覧のフィルタ** (テキストレイヤだけ / 名前で絞り込み)。
  表示 ON/OFF とは別で、一覧そのものを絞る
- **Ctrl+Shift+Enter で「反映して次のテキストレイヤへ」**。移動した直後は本文が
  全選択されているので、そのまま打ち替えられる。Ctrl+&uarr; / Ctrl+&darr; で上下移動
- **CSV は画面へドロップして読み込める**。PSD のほうはブラウザの画面へ落として
  も中身しか渡らず (パスが取れないので exe 側から開けない)、psdtext.exe やその
  ショートカットへ落とす形になる。画面へ落としたときはその案内を出す
- **ウィンドウの大きさと位置を覚える**
- パスを貼り付けようとしてダイアログの外でマウスを離すと閉じてしまうのを修正

---

## 0.1.1 — 2026-08-14

- **書式マークで編集する形に作り直した**。本文にタグを出さず、書式が変わる位置に
  マーク (◆) だけを表示する。カーソルがいる位置のマークがそのまま編集対象になり、
  マークの削除もできる
- **色を指定できるようになった**
- **読み込んだときの書式 (基準) と、途中の書式を分けた**。多くの場合は基準だけ
  変えたいので、基準は選ぶだけ、マークや範囲はボタンから選ぶ形にした
- **範囲を選んで書式を変えると閉じタグが入っていた**のをやめ、範囲の後ろを
  元の書式に戻す指定を置く形にした
- **位置の編集欄を位置表示のすぐ下に移した**。変更前の値が上に残るので見比べられる
- **書式が他のレイヤに移ってしまう / 元に戻せなくなる不具合を修正**。基準を
  読み込み時で固定し、ランの書式を絶対値で書き戻すようにした
- **psdtext を終了するとブラウザ側の画面も自動で閉じる**
- 書式マークの組み替えに単体テストを付けて CI で走らせる

## 0.1.0 — 2026-08-14

最初のリリース。

- PSD のテキストレイヤを一覧して本文を書き換え、保存し直せる
- 合成プレビュー、階層の表示 ON/OFF、テキストの仮描画
- 書式付きテキストのタグ表現、行揃え、レイヤの複製・移動・名前の変更
- テキストの位置と流し込み枠の編集 (数値 / ドラッグ / 矢印キー)
- CSV の書き出しと取り込み
- 内蔵ヘルプ、UI の日英切り替え
- Windows / Linux の配布パッケージを GitHub Actions で作る
